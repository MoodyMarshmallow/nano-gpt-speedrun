# Baseline (no gating) — mirrors script defaults
# Usage: set -a; source .example.baseline.env; set +a

# Disable gating
ATTNGATE=none
GATEPOS=sdpa
GATEACT=sigmoid

# Core hyperparams (match defaults)
LR=0.0036
SEED=1337

# Optional metadata
RUNPOD_INSTANCE_TYPE=A10G
