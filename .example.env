# Example overrides for quick sweeps; `set -a; source .example.env; set +a`
ATTNGATE=headwise    # attention gate type: none|headwise (per-head scalar)|elementwise (per-head dim)|const (learned static per-head)
GATEPOS=sdpa         # where to apply gate: sdpa (after attention output) | value (on V before attention)
GATEACT=sigmoid      # gate activation: sigmoid (sparse) | ns_sigmoid (non-sparse control)
LR=0.0036            # learning rate override
SEED=1337            # RNG seed for torch/cuda/numpy
RUNPOD_INSTANCE_TYPE=A10G  # optional: logged hardware metadata if on RunPod
